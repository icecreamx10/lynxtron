dependencies {
}

android {
  compileSdkVersion 35
  buildToolsVersion "35.0.0"

  defaultConfig {
    minSdkVersion 24
    targetSdkVersion 35
    versionCode 1
    versionName "0.0.1"
    generatedDensities = []
  }

  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
